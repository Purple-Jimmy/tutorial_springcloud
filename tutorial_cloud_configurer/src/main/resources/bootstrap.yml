server:
  port: 8080
  application:
    name: config-server # 配置中心的名字
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/config  # 配置文件所在目录,classpath(类路径)和(系统文件路径) file两种